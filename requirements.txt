# Core Dependencies (v1.1-clean)
# The minimal core loop has ZERO external dependencies.
# It runs on Python 3.10+ stdlib only.

# Future production extensions may require:
# flask>=3.0.0              # REST API
# prometheus-client>=0.19.0  # Metrics export
# python-telegram-bot>=20.8  # Alerts
```
